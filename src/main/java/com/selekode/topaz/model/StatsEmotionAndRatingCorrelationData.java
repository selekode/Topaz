package com.selekode.topaz.model;

public class StatsEmotionAndRatingCorrelationData {
	// Creates a Structured Data Object: StatsEmotionAndRatingCorrelationData holds two 2D arrays (double[][]):
	public double[][] emotions;
    public double[][] ratings;
    
    // emotions → Each row represents an entry, each column an emotion.
    // ratings → Each row represents an entry, each column a rating.
}
